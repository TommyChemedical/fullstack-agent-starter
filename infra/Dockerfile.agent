FROM python:3.11-slim
RUN useradd -m appuser
WORKDIR /opt/agent
COPY agent/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY agent /opt/agent
USER appuser
CMD ["python","-c","print('agent ready')"]
